import {DataTypes, Model} from "../deps.ts";

/*
export class ExperienceManager extends Model{
    static table: 'experienceManager';

    static field = {
        id: {
            type: DataTypes.INTEGER,
            primaryKey: true,
            autoIncrement: true,
        },

    }

    //level: number;
    //xp: number;
    //userId: number;
}
 */